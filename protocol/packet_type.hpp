#pragma once

#include <cstdint>

enum class PacketType : uint32_t {
  PING = 1,
  PONG = 2,

  AUTH_REQUEST = 3,
  AUTH_RESPONSE = 4,

  LIST = 5,
  LIST_RESPONSE = 6,

  UPLOAD_BEGIN = 7,
  UPLOAD_CHUNK = 8,
  UPLOAD_END = 9,

  DOWNLOAD_REQUEST = 10,
  DOWNLOAD_CHUNK = 11,

  DELETE_FILE = 12,
  RENAME_FILE = 13,
  MKDIR = 14,

  SUCCESS = 15,
  ERROR = 16,

  UPLOAD_ACK = 17,

  DOWNLOAD_BEGIN = 18,
  DOWNLOAD_END = 19,

  DELETE_REQUEST = 20,
  DELETE_RESPONSE = 21,

  RENAME_REQUEST = 22,
  RENAME_RESPONSE = 23,

  MKDIR_REQUEST = 24,
  MKDIR_RESPONSE = 25,

  RMDIR_REQUEST = 26,
  RMDIR_RESPONSE = 27
};
